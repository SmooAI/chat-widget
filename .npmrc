# pnpm resolves inject-workspace-packages differently depending on the
# environment (true locally, false on the CI runner), which made a lockfile
# generated in one place fail `--frozen-lockfile` in the other. This repo has
# no workspace, so the value is behaviourally irrelevant — it just has to be
# stated rather than inherited.
inject-workspace-packages=false
